底部导航带来两种浏览方式：时间线按日期平铺全部内容，列表则为每个合集一张卡片，点开查看成员。卡片视图仍保留在「设置 > 外观」中。

全客户端统一的离线状态，只用一条琥珀色提示条和播放器内的失败提示来报告——断链不再关闭播放器。页面数超过浏览器连接上限时会明确提示，而不是空白卡住。

卡片操作收进一个溢出菜单，每项都有名称。隐私模糊的解除改为临时性，应用切到后台一段时间后自动恢复。启动图标视觉居中。
