{
  "name": "orca-userscript",
  "version": "0.2.0",
  "private": true,
  "description": "Orca userscript (Tampermonkey / Violentmonkey): submit videos to your self-hosted Orca over the same E2EE secure channel as the web UI, and download them straight from any video page.",
  "type": "module",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "build": "tsx build-userscript.ts",
    "typecheck": "tsc --noEmit",
    "dist": "npm run typecheck && npm run build",
    "verify:e2ee": "tsx scripts/verify-e2ee.ts"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.4",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
